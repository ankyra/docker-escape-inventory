name: docker-escape-registry
version: $registry.version
description: Docker image for the Escape Registry, which gets pushed to
    https://hub.docker.com/r/ankyra/escape-registry/

logo: logo.png
extends:
- extension-docker-latest

depends:
- escape-registry-latest as registry

inputs:
- default: escape-registry
  id: docker_image

includes:
- Dockerfile
- default-config.json
- LICENSE
