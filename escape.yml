name: docker-escape-registry
version: $registry.version
description: Docker image for the Escape Registry

extends:
- extension-docker-latest

depends:
- escape-registry-latest as registry

inputs:
- default: escape-registry
  id: docker_image

includes:
- Dockerfile
- default-config.json
- LICENSE
